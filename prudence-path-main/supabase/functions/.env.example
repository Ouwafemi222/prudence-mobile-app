# Copy to supabase/.env.local for: supabase functions serve --env-file supabase/.env.local
# Production: Supabase Dashboard → Project Settings → Edge Functions → Secrets

# --- Required for Resend emails (submission approve/reject only; in-app alerts do not email) ---
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@yourdomain.com
RESEND_FROM_NAME=THE PRUDENCE

# --- Required for DB access (match your Dashboard secret names) ---
PROJECT_URL=https://xpvabdfleomjpytvvjux.supabase.co
SERVICE_ROLE_KEY=your-service-role-key

# --- Required for "Open in app" links in emails ---
SITE_URL=https://prudence-path.online

# --- Admin notification emails (signup + approval alerts) ---
ADMIN_EMAIL=agboola378@gmail.com
# Alternate (preview): https://prudence-path.vercel.app
